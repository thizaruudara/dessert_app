import 'package:flutter/material.dart';
// Placeholder widget — phone flag picker can be added with country_picker package
class PhoneFlagField extends StatelessWidget {
  const PhoneFlagField({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
